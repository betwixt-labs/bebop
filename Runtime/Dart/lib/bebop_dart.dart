/// This library provides the classes that the `bebop` Dart codegen output relies on.
///
/// You shouldn't need to access these classes directly to use Bebop. Instead, add
/// this package as a dependency and then use the `encode` and `decode` methods in
/// the code that the Bebop compiler generates for your schemas.
library bebop_dart;

export 'src/bebop.dart';

